Folder structure:

```
app/
    domain/      defines application (internal) object interfaces/logic
    repository/  defines interactions with the database
    routes/      defines api routes exposed by the service
    schemas/     defines input schemas required for external requests
```
